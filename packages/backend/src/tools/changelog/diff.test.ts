import { getContractValue } from '../../api/controllers/discovery/utils'
import { getDiscoveryChanges } from './diff'

describe(getDiscoveryChanges.name, () => {
  it('works', () => {})
})
describe(getContractValue.name, () => {
  it('works', () => {})
})
